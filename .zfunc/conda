# shellcheck disable=all

unfunction conda 2>/dev/null

if command -v conda &>/dev/null; then
	:
elif [[ -x $HOME/anaconda3/condabin/conda ]]; then
	export CONDA_EXE="$HOME/anaconda3/bin/conda"
	path=("$HOME/anaconda3/condabin" $path)
else
	function conda() {
		mamba "$@"
	}
fi

conda "$@"
